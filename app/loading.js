/**
 * Loading component displayed while products are being fetched or loaded.
 *
 * @component
 * @example
 * return <Loading />;
 *
 * @returns {JSX.Element} The rendered Loading component.
 */
export default function Loading() {
  return <div>Loading products...</div>;
}
